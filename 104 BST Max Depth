class Solution {
    public int maxDepth(TreeNode root) {
        if (root == null) return 0; // Base case: empty tree
        int left = maxDepth(root.left);
        int right = maxDepth(root.right);
        return Math.max(left, right) + 1; // add current node
    }
}
